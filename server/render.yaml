services:
  - type: web
    name: scorcorect-server
    runtime: node
    buildCommand: npm install && npx prisma generate && npx prisma db push --skip-generate
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
